  第一阶段：Python 基础 + 项目骨架（1周）

  ┌────────┬─────────────────────────────────────────┬────────────────────┐
  │ 优先级  │                  内容                    │    对应项目模块       │
  ├────────┼─────────────────────────────────────────┼────────────────────┤
  │ 1      │ Python 基础语法（函数、类、模块、异常）        │ 所有                │
  ├────────┼─────────────────────────────────────────┼────────────────────┤
  │ 2      │ 异步编程 async/await                      │ api/main.py        │
  ├────────┼─────────────────────────────────────────┼────────────────────┤
  │ 3      │ Pydantic 数据验证                         │ schemas/           │
  ├────────┼─────────────────────────────────────────┼────────────────────┤
  │ 4      │ FastAPI 路由、依赖注入                     │ api/main.py        │
  ├────────┼─────────────────────────────────────────┼────────────────────┤
  │ 5      │ 环境变量管理 python-dotenv                 │ config/settings.py │
  └────────┴─────────────────────────────────────────┴────────────────────┘

  项目任务： 完成 schemas/models.py + api/main.py 的基础框架

  ---
  第二阶段：核心服务层（2周）

  ┌────────┬─────────────────────────────────────┬────────────────────────────┐
  │ 优先级  │                内容                  │        对应项目模块           │
  ├────────┼─────────────────────────────────────┼────────────────────────────┤
  │ 1      │ LLM Service 调用（阿里云百炼SDK封装）    │ services/llm_service.py    │
  ├────────┼─────────────────────────────────────┼────────────────────────────┤
  │ 2      │ Redis Vector Search                 │ services/vector_service.py │
  ├────────┼─────────────────────────────────────┼────────────────────────────┤
  │ 3      │ Neo4j 图数据库查询                    │ services/graph_service.py  │
  └────────┴─────────────────────────────────────┴────────────────────────────┘

  项目任务： 完成 services/ 模块（LLM/向量/图谱三个核心服务）

  ---
  第三阶段：Embeddings 向量化层（1周）

  ┌────────┬─────────────────────────────────────┬────────────────────────────┐
  │ 优先级  │                内容                  │        对应项目模块           │
  ├────────┼─────────────────────────────────────┼────────────────────────────┤
  │ 1      │ 文本向量化（百炼text-embedding）       │ embeddings/text_embedding.py│
  ├────────┼─────────────────────────────────────┼────────────────────────────┤
  │ 2      │ 图像向量化（CLIP）                    │ embeddings/image_embedding.py│
  ├────────┼─────────────────────────────────────┼────────────────────────────┤
  │ 3      │ 多模态统一向量                        │ embeddings/multimodal_embedding.py│
  └────────┴─────────────────────────────────────┴────────────────────────────┘

  项目任务： 完成 embeddings/ 模块

  ---
  第四阶段：Tools 层（1-2周）

  ┌────────┬──────────────────────────┬───────────────────────────────────┐
  │ 优先级  │           内容            │           对应项目模块               │
  ├────────┼──────────────────────────┼───────────────────────────────────┤
  │ 1      │ Base Tool 抽象            │ tools/base_tool.py                │
  ├────────┼──────────────────────────┼───────────────────────────────────┤
  │ 2      │ 知识检索工具（向量搜索）      │ tools/knowledge_retrieval_tool.py │
  ├────────┼──────────────────────────┼───────────────────────────────────┤
  │ 3      │ 图查询工具                 │ tools/graph_query_tool.py         │
  ├────────┼──────────────────────────┼───────────────────────────────────┤
  │ 4      │ YOLO 检测工具（远程调用）    │ tools/yolo_tool.py                │
  ├────────┼──────────────────────────┼───────────────────────────────────┤
  │ 5      │ SAM 分割工具（远程调用）    │ tools/sam_tool.py                 │
  └────────┴──────────────────────────┴───────────────────────────────────┘

  项目任务： 完成 tools/ 全部工具（模板/缓存工具已移除，由Java管理）

  ---
  第五阶段：Agent + Chain（2-3周）

  ┌────────┬────────────────────────────────────┬──────────────────────────────┐
  │ 优先级  │                内容                 │         对应项目模块            │
  ├────────┼────────────────────────────────────┼──────────────────────────────┤
  │ 1      │ Base Agent 抽象                     │ agents/base_agent.py         │
  ├────────┼────────────────────────────────────┼──────────────────────────────┤
  │ 2      │ LangChain Chain 编写                │ chains/                      │
  ├────────┼────────────────────────────────────┼──────────────────────────────┤
  │ 3      │ Orchestrator Agent（意图识别+调度）   │ agents/orchestrator_agent.py │
  ├────────┼────────────────────────────────────┼──────────────────────────────┤
  │ 4      │ Retrieval/Diagnosis/Guidance Agent │ agents/                      │
  └────────┴────────────────────────────────────┴──────────────────────────────┘

  项目任务： 完成 agents/ 和 chains/ 模块

  ---
  第六阶段：与Java后端集成（1-2周）

  ┌────────┬────────────────────────────────────┬──────────────────────────────┐
  │ 优先级  │                内容                 │         说明                   │
  ├────────┼────────────────────────────────────┼──────────────────────────────┤
  │ 1      │ API接口对接                         │ 定义清晰的请求/响应模型         │
  ├────────┼────────────────────────────────────┼──────────────────────────────┤
  │ 2      │ 会话上下文传递                      │ Java传递历史，Python不管理      │
  ├────────┼────────────────────────────────────┼──────────────────────────────┤
  │ 3      │ 错误处理规范                        │ 返回结构化错误码               │
  └────────┴────────────────────────────────────┴──────────────────────────────┘

  项目任务： 完成与Java后端的集成对接

  ---
  第七阶段：收尾（1周）

  - API 路由完善
  - 错误处理
  - Docker 部署
  - 文档更新

  ---
  关键依赖关系图

  schemas (数据模型)
      ↓
  config (配置)
      ↓
  services (LLM/Redis/Neo4j) + embeddings (向量化)
      ↓
  tools (工具)
      ↓
  agents (Agent) + chains (Chain)
      ↓
  api (API层)
      ↓
  Java Backend (调用方)

  ---
  开发原则

  1. 边界清晰：Python只负责AI推理，不碰业务数据
  2. 接口极简：Java传递最小上下文，Python返回结构化结果
  3. 配置独立：AI模块配置独立管理，不依赖Java配置中心
  4. 错误友好：返回结构化错误码，Java负责用户友好提示

  ---
  建议： 按上述顺序开发，从 schemas → config → services → embeddings → tools → agents → chains → api 的依赖顺序推进。每个阶段先看 README.md，再上手写代码。
